.card(ng-repeat="item in contentList.list3")
	.card__pic
		img.card__pic-img#img-height(ng-src="{{item.src}}")
	.card__info
		.card__header {{item.header}}
		.card__price {{item.price}}
		.card__art {{item.art}}
		.card__art {{item.weight}}
	.card__btn
		.card__btn-1
			a.card__btn-1-link(href="{{item.href}}") Подробнее